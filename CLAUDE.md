# CLAUDE.md — プロジェクトルール

## ユーザーのGit知識について

ユーザー（shinomiyapiyo）はGit/GitHub/Commit/Push/Pullの操作にほとんど詳しくありません。

以下のルールを必ず守ること：

1. **反映に必要な操作は毎回説明する** — 「Pushしました」だけで終わらず、その後ユーザーが何をすべきか（マージボタンを押す等）を毎回具体的に説明すること
2. **可能な限りClaudeが操作を行う** — Push、Commit、ブランチ作成などClaudeが実行できる操作は、確認を取った上でClaudeが代行すること
3. **ユーザーに操作を求める場合はスクリーンショット級の詳細手順を示す** — 「PRをマージしてください」ではなく、どこをクリックするか具体的に書くこと
4. **専門用語を使う場合は簡単な説明を添える** — 例：「Push（あなたの変更をGitHubにアップロードすること）」

## プロジェクト構成

- `games/piyo-adventure/` — ぴよアドベンチャーのメインゲーム
- `games/piyo-adventure/index.html` — ゲーム本体（メインロジック）
- `games/piyo-adventure/i18n.js` — 多言語テキスト辞書（ja/en）＋翻訳関数
- `games/piyo-adventure/audio.js` — SoundManagerクラス（BGM/SE）
- `games/piyo-adventure/sprites.js` — スプライト定義

## 操作範囲の制限

- **操作可能な場所は以下の3つのうちいずれか1つのみ：**
  1. `games/piyo-adventure/`
  2. `games/14banchi/`
  3. ルートの `index.html` および `images/` フォルダ（この場合 `games/` フォルダは編集不可）
- **1つのセッション（会話）内では、最初にアクセスした場所のみ操作可能** — 他の2つの場所の作成・編集・削除は禁止
- セッション開始時にユーザーの最初の指示内容から対象を判断し、そのセッション中はその場所だけで作業すること
- 上記3か所以外のディレクトリやファイルの作成・編集・削除は禁止（この `CLAUDE.md` の更新を除く）

## ライブ情報の管理ルール

- **過去のライブ情報はHTMLから削除しない** — 日付が過ぎたイベントはJavaScriptで自動的に非表示になるため、コードからの削除は不要。データとして残しておくこと

## リリース目標

- **App Store（iOS）と Google Play Store（Android）でのリリースを目指して開発中**
- Capacitor を使用して Web アプリをネイティブアプリ化する予定
- ストア審査基準（プライバシーポリシー、利用規約、課金実装、広告SDK、年齢制限表示等）を常に意識すること
- 広告SDK（AdMob等）・課金（IAP）は現在 stub 実装。Capacitor 導入時にネイティブプラグインに差し替える

## 開発ルール

- ゲームは `index.html` ＋ 分割jsファイル（`sprites.js` / `i18n.js` / `audio.js`）の構成。ビルドツールは使わず `<script src>` で読み込む
- **新しいjsファイルを追加したら `sw.js` の `STATIC_ASSETS` に必ず登録する** — 忘れるとオフライン時に壊れる
- jsファイルの読み込み順序: sprites.js → i18n.js → audio.js → index.html内のメインスクリプト（依存方向は常にメイン側から分割ファイル側へ）
- 変更後は必ず動作確認の観点を説明すること
- 貯金システムが実装されるまで、タイトルショップはグレーアウト（無効化）を維持すること
- **HTMLファイルを1行でも変更したらバージョンを +0.001 上げる** — ユーザーへの確認は不要、自動的に行うこと
- **`index.html` のバージョンを上げたら `sw.js` の `CACHE_NAME` も必ず同時に更新する** — PWAのキャッシュ更新に必須。これを忘れるとユーザーに古いバージョンが配信され続ける
- **回答の末尾に現在のバージョンを必ず記載する** — 形式：`（現在のバージョン: Ver.X.XXX）` — ユーザーが確認しているファイルが最新かどうか判断できるようにするため必須

## 素材ライセンスメモ（導入検討中）

### OtoLogic（効果音・BGM素材）
- **サイト**: https://otologic.jp
- **ライセンス**: CC BY 4.0（クレジット表記で無料利用可）
- **商用・アプリ利用**: OK
- **必須**: クレジット表記（例：`効果音素材：OtoLogic（https://otologic.jp） / CC BY 4.0`）
- **クレジット不要にしたい場合**: 有償ライセンス（効果音1点 550円、ジングル 1,100円、BGM 2,200円・税別）
- **利用規約**: https://otologic.jp/free/license.html
- **導入時の対応**: アプリ内（設定画面またはクレジット欄）にクレジット表記を追加すること
- **ステータス**: 未使用（2026-03-04時点）。導入する場合はクレジット表記の実装が必要

## ストアリリース進捗チェックリスト

タスク完了時はClaudeが `[ ]` → `[x]` に更新し、日付を記入すること。

### 法的ページ・規約

- [x] **プライバシーポリシー** — 外部ページ作成済み、ゲーム内リンク済み（設定画面） (2026-03-03)
- [x] **利用規約テキスト作成** — 日本語・英語テキスト作成済み、ユーザーに提供済み (2026-03-03)
- [x] **利用規約リンク（ゲーム内）** — 設定画面にリンク追加済み (2026-03-03)
- [x] **利用規約ページ公開** — ユーザーが公開完了 (2026-03-03)

### ゲーム内機能の完成

- [ ] **コインマスター機能** — 現在「準備中」表示。スターターパック購入後に解放される設計
- [ ] **Ad-Free（広告非表示）機能の有効化** — 現在 `display:none` で非表示。課金実装後に有効化
- [ ] **スキン機能** — コードに「将来追加」とコメントあり。タイトルショップの拡張要素

### Capacitor・ネイティブ化

- [ ] **Capacitor導入** — Web→ネイティブアプリ化の基盤。広告・課金の前提条件
- [ ] **広告SDK統合（AdMob）** — 現在stub。Capacitorプラグインに差し替え
- [ ] **課金SDK統合（IAP）** — 現在stub（`purchaseItem()`が常に成功を返す）。Apple StoreKit / Google Play Billing に差し替え
- [ ] **Apple App Tracking Transparency (ATT)** — iOS広告表示に必須

### ストア申請要件

- [ ] **年齢制限表示（IARC）** — ストア申請時に必要
- [ ] **アプリアイコン（各サイズ）** — PWA用は192/512あり。ストア用の追加サイズが必要か確認
- [ ] **スクリーンショット（ストア掲載用）** — 各デバイスサイズのスクリーンショット
- [ ] **アプリ説明文（ストア掲載用）** — 日本語・英語
- [ ] **データバックアップ機能** — デバイス変更時のデータ引き継ぎ（審査で問われる可能性）

### 品質・バランス

- [ ] **ゲームバランス最終調整** — アイテム価格、ボス難易度等
- [ ] **全体テスト** — 全ラウンド通しプレイ、エッジケース確認

## 14番地 ゲーム改善バックログ

ゲーム評価（2026-06-26）で挙がった改善候補。優先度順。着手時に Claude が `[ ]` → `[x]` に更新し日付を記入する。

- [ ] **【最優先】ノーマルモードの救済措置** — 1ミスで1番地に逆戻りは新規プレイヤーに厳しい（14番地クリアに実質12連続正解が必要）。チェックポイント（数番地ごと）または序盤の数ミス許容などを検討。新規離脱の低減が狙い。
- [ ] **イージーモードの追加** — 難易度の低い異変だけが出る新モード。カジュアル/新規向けの入口（上記の難易度課題への直接的な対策にもなる）。※ユーザー提案 (2026-06-26)
- [ ] **微妙すぎる異変の手当て** — 直前の正常な景色をチラ見できる機能／難易度別の異変プール／該当異変（点線の長さ・街灯の高さ・短い道 等）の手がかり強化。「理不尽」という声を予防。
- [ ] **オンボーディング強化** — 即死異変・モードの違い・ハート回復などを最初の数プレイで軽くガイド。
- [ ] **アクセシビリティ** — 色だけに頼る異変（赤い空/青い地面/白・オレンジ街灯など）に形・アイコンの手がかりを付与。明るさ案内をもう一段わかりやすく。
- [x] **差別化の打ち出し** — ストア説明文に「41種・3モード・ランキング」を反映済み (2026-06-26)

## 変更禁止事項

- **デッドゾーン（`.control-dead` の幅 37%、`#leftArea`〜`#jumpArea` 間の中央空白領域）は絶対に変更禁止** — 誤タップによるアプリ終了を防ぐための安全領域。L/Rタッチ領域の拡大時もデッドゾーンのサイズは維持すること

## Git操作ルール

- **Git/Pull/Push/Commit/Merge はすべてClaudeが実行する** — ユーザーへの確認なしで可能な限り操作を完了させること
- Claudeが操作を完了できなかった場合は、**ユーザーが何をすべきかを具体的に分かりやすく伝える**こと
