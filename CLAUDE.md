# CLAUDE.md — プロジェクトルール

## ユーザーのGit知識について

ユーザー（shinomiyapiyo）はGit/GitHub/Commit/Push/Pullの操作にほとんど詳しくありません。

以下のルールを必ず守ること：

1. **反映に必要な操作は毎回説明する** — 「Pushしました」だけで終わらず、その後ユーザーが何をすべきか（マージボタンを押す等）を毎回具体的に説明すること
2. **可能な限りClaudeが操作を行う** — Push、Commit、ブランチ作成などClaudeが実行できる操作は、確認を取った上でClaudeが代行すること
3. **ユーザーに操作を求める場合はスクリーンショット級の詳細手順を示す** — 「PRをマージしてください」ではなく、どこをクリックするか具体的に書くこと
4. **専門用語を使う場合は簡単な説明を添える** — 例：「Push（あなたの変更をGitHubにアップロードすること）」

## プロジェクト構成

- `games/piyo-adventure/` — ぴよアドベンチャーのメインゲーム
- `games/piyo-adventure/index.html` — ゲーム本体（単一HTMLファイル）

## 操作範囲の制限

- **`games/piyo-adventure/` 内のファイルのみ操作可能** — それ以外のディレクトリやファイルの作成・編集・削除は禁止（この `CLAUDE.md` の更新を除く）

## リリース目標

- **App Store（iOS）と Google Play Store（Android）でのリリースを目指して開発中**
- Capacitor を使用して Web アプリをネイティブアプリ化する予定
- ストア審査基準（プライバシーポリシー、利用規約、課金実装、広告SDK、年齢制限表示等）を常に意識すること
- 広告SDK（AdMob等）・課金（IAP）は現在 stub 実装。Capacitor 導入時にネイティブプラグインに差し替える

## 開発ルール

- ゲームは単一HTMLファイル（`index.html`）に全コードを含む構成
- 変更後は必ず動作確認の観点を説明すること
- 貯金システムが実装されるまで、タイトルショップはグレーアウト（無効化）を維持すること
- **HTMLファイルを1行でも変更したらバージョンを +0.001 上げる** — ユーザーへの確認は不要、自動的に行うこと
- **`index.html` のバージョンを上げたら `sw.js` の `CACHE_NAME` も必ず同時に更新する** — PWAのキャッシュ更新に必須。これを忘れるとユーザーに古いバージョンが配信され続ける
- **回答の末尾に現在のバージョンを必ず記載する** — 形式：`（現在のバージョン: Ver.X.XXX）` — ユーザーが確認しているファイルが最新かどうか判断できるようにするため必須

## 素材ライセンスメモ（導入検討中）

### OtoLogic（効果音・BGM素材）
- **サイト**: https://otologic.jp
- **ライセンス**: CC BY 4.0（クレジット表記で無料利用可）
- **商用・アプリ利用**: OK
- **必須**: クレジット表記（例：`効果音素材：OtoLogic（https://otologic.jp） / CC BY 4.0`）
- **クレジット不要にしたい場合**: 有償ライセンス（効果音1点 550円、ジングル 1,100円、BGM 2,200円・税別）
- **利用規約**: https://otologic.jp/free/license.html
- **導入時の対応**: アプリ内（設定画面またはクレジット欄）にクレジット表記を追加すること
- **ステータス**: 未使用（2026-03-04時点）。導入する場合はクレジット表記の実装が必要

## Git操作ルール

- **Git/Pull/Push/Commit/Merge はすべてClaudeが実行する** — ユーザーへの確認なしで可能な限り操作を完了させること
- Claudeが操作を完了できなかった場合は、**ユーザーが何をすべきかを具体的に分かりやすく伝える**こと
