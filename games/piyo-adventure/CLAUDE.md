# Scope Rule

This session ONLY works within the `games/piyo-adventure/` directory.

- Do NOT read, edit, or create files outside of `games/piyo-adventure/`.
- All file operations (Read, Edit, Write, Glob, Grep) must target paths under `games/piyo-adventure/`.
- If a request would require changes outside this directory, ask the user for confirmation first.
