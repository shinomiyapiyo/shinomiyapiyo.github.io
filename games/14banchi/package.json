{
  "name": "banchi14",
  "version": "1.0.0",
  "private": true,
  "description": "14番地 (iOS native via Capacitor)",
  "scripts": {
    "copy:web": "rm -rf www && mkdir -p www && cp index.html sw.js native-bridge.js manifest.json icon-192.png icon-512.png www/ && cp -R images audio www/",
    "sync": "npm run copy:web && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor-community/admob": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
